void Inventory(){
    int x1,y1,z1,t1;
    int x1=("?? ?? ?? ??");
    int y1=("$$ $$ $$ $$");
    int z1=("!! !! !! !!");
    int t1=("@@ @@ @@ @@");
    do (x1 * y1 * z1) / t1;
    for(x1=0;x1=<1;++x1);
    for(y1=0;y1=<1;++y1);
    for(z1=0;z1=<1;++z1);
       while(x1%y1%z1);
       goto Inventory;

    int x2,y2,z2,t2;
    int x2=("__ __ __ __");
    int y2=("~~ ~~ ~~ ~~");
    int z2=("|| || || ||");
    int t2=("== == == ==");
    do ( x2 * y2 * z2 ) / t2;
    for(x2=0;x2<=2;++x2);
    for(y2=0;y2<=2;++y2);
    for(z2=0;z2<=2;++z2);
        while(x2%y2%z2);
        goto Inventory;

    double z1,z2,z3,z4;
    char z1=("<><> <><> <><> <><>");
    char z2=("()() ()() ()() ()()");
    char z3=("[][] [][] [][] [][]");
    char z4=("{}{} {}{} {}{} {}{}");
    do z1 * z2 * z3 * z4;
    for(z1=0;z1<=4;++z1);
    for(z2=0;z2<=4;++z2);
    for(z3=0;z3<=4;++z3);
    for(z4=0;z4<=4;++z4);
    while (z1 % z2 % z3 % z4)
    
   long double z11,z22,z33,z44;
   char z11=("++ ++ ++ ++");
   char z22=("-- -- -- --");
   char z33=("** ** ** **");
   char z44=("%% %% %% %%");
    for(z11=0;z11<=8;++z11);
    for(z22=0;z22<=8;++z22);
    for(z33=0;z33<=8;++z33);
    for(z44=0;z44<=8;++z44);
      while(11 % 22 % 33 % 44);
      goto Inventory;
}