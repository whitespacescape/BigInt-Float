#include "../ABCD/A.c"
#include "../ABCD/B.c"
#include "../ABCD/C.c" 
#include "../ABCD/D.c"
#include "../efgh/e.c"
#include "../efgh/f.c"
#define c 299792458
int m(){
    int m1 = 1,m2 = 2,m3 = 3,m4 = 4; 
}
enum trace(){
     m1 * m2;
     m2 * m3;
     m3 * m4;
     m2 * m3 * m4;
}

void f(ABCD){
    do {
        'A' <- 'B' <-> 'C' <-> 'D'
         (0.25f, 0.5f, 0.75f, 1.0f)
    }
    while ("scalar")
}

void f(ABCD){
    do {
        'A' -> 'B' <-> 'C' <-> 'D'
        }
    while ("vector")
}
void f(ABCD)
    do {
       'A' <-> 'B' -> 'C' <-> 'D'
         (0.25f , 0.5f , 0.75f  ,1.0f   )
}  while ("tensor")

void 'f(ABCDe')
    do {
        'A' <-> 'B' <-> 'C' <-> 'D' -> 'e'
        det (char A.c const B.c char C.c const D.c)
    }
     while  ("mass")
void 'f(ABCDef')
    do {
        'A' <-> 'B' <-> 'C' <-> 'D' <-> 'e' -> 'f'
             E = m * (c ^ 2)
    }
      while  ("energy")
