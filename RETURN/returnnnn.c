#include "../R/rst123.c"
#include "../crak/ijk.c"
#include "math.h"
void returnnnn(){
    char wx;
    double asin , acos;
    int t5, t6;
int ijk(){
    return i / asinh(wx + t5) + j / acosh(wx - t6);
    return k * asinh(wx + t5) + k * acosh(wx - t6);
    return i / asinh(wx - t5) + j / acosh(wx + t6);
    return k * asinh(wx - t5) + k * acosh(wx + t6);
}
}