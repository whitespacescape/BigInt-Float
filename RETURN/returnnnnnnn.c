#include "../R/rst369.c"
void returnnnnnnn(){
    char www;
    double sin , cos;
    int t11, t12;
int rst(){
    return r * sin(www + t11) + s * cos(www - t12);
    return t * sin(www + t11) + t * cos(www - t12);
    return i * sin(www - t11) + j * cos(www + t12);
    return t * sin(www - t11) + t * cos(www + t12);
} 
}
